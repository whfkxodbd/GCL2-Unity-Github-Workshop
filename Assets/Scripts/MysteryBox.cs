using UnityEngine;

public class MysteryBox : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Mystery box: Activated! Gain 3 EXP!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
